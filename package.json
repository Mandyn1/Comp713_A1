{
  "name": "comp713_a1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev-all": "concurrently \"cd webapp && npm run dev\" \"cd server && npm run dev\"",
    "install-all": "npm install && concurrently \"cd webapp && npm install\" \"cd server && npm install\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
